## 1. Project Initialization

- [x] 1.1 Run `npx create-expo-app@latest . --template blank-typescript` inside `apps/mobile`
- [x] 1.2 Verify `npx expo start` launches Metro without errors
- [x] 1.3 Confirm `tsc --noEmit` passes with zero errors on the generated files

## 2. Navigation Setup

- [x] 2.1 Install `expo-router` and its peer dependencies (`expo install expo-router`)
- [x] 2.2 Update `app.json` to set `"main": "expo-router/entry"` as the entry point
- [x] 2.3 Create `app/_layout.tsx` as the root layout wrapping `<Slot />`
- [x] 2.4 Create `app/index.tsx` as a placeholder home screen rendering a `<Text>` element
- [x] 2.5 Verify the index screen renders on iOS simulator and Android emulator without crash

## 3. SQLite Integration

- [x] 3.1 Install `expo-sqlite` (`expo install expo-sqlite`)
- [x] 3.2 Create `src/db/index.ts` that opens the database once and exports the singleton instance
- [x] 3.3 Implement migration runner in `src/db/migrate.ts` that creates `_migrations` table and applies pending migrations in order
- [x] 3.4 Wire migration runner to run during app initialization (e.g., in root `_layout.tsx` or app entry)
- [x] 3.5 Add a smoke-test migration (e.g., create an empty `app_meta` table) to verify the runner works end-to-end

## 4. EAS Configuration

- [x] 4.1 Run `eas build:configure` to generate `eas.json`
- [x] 4.2 Confirm `eas.json` includes `development`, `preview`, and `production` profiles
- [x] 4.3 Verify `eas build --help` reports no configuration errors

## 5. Cleanup & Commit

- [x] 5.1 Remove unused boilerplate files generated by `create-expo-app` (e.g., default `App.tsx` if replaced by router)
- [x] 5.2 Run `tsc --noEmit` and ensure zero errors
- [x] 5.3 Run `npx expo start` and do a final smoke-test on both platforms
- [x] 5.4 Commit with message `[chore] initialize expo + sqlite project`
