## 1. Project Initialization

- [ ] 1.1 Run `npx create-expo-app@latest . --template blank-typescript` inside `apps/mobile`
- [ ] 1.2 Verify `npx expo start` launches Metro without errors
- [ ] 1.3 Confirm `tsc --noEmit` passes with zero errors on the generated files

## 2. Navigation Setup

- [ ] 2.1 Install `expo-router` and its peer dependencies (`expo install expo-router`)
- [ ] 2.2 Update `app.json` to set `"main": "expo-router/entry"` as the entry point
- [ ] 2.3 Create `app/_layout.tsx` as the root layout wrapping `<Slot />`
- [ ] 2.4 Create `app/index.tsx` as a placeholder home screen rendering a `<Text>` element
- [ ] 2.5 Verify the index screen renders on iOS simulator and Android emulator without crash

## 3. SQLite Integration

- [ ] 3.1 Install `expo-sqlite` (`expo install expo-sqlite`)
- [ ] 3.2 Create `src/db/index.ts` that opens the database once and exports the singleton instance
- [ ] 3.3 Implement migration runner in `src/db/migrate.ts` that creates `_migrations` table and applies pending migrations in order
- [ ] 3.4 Wire migration runner to run during app initialization (e.g., in root `_layout.tsx` or app entry)
- [ ] 3.5 Add a smoke-test migration (e.g., create an empty `app_meta` table) to verify the runner works end-to-end

## 4. EAS Configuration

- [ ] 4.1 Run `eas build:configure` to generate `eas.json`
- [ ] 4.2 Confirm `eas.json` includes `development`, `preview`, and `production` profiles
- [ ] 4.3 Verify `eas build --help` reports no configuration errors

## 5. Cleanup & Commit

- [ ] 5.1 Remove unused boilerplate files generated by `create-expo-app` (e.g., default `App.tsx` if replaced by router)
- [ ] 5.2 Run `tsc --noEmit` and ensure zero errors
- [ ] 5.3 Run `npx expo start` and do a final smoke-test on both platforms
- [ ] 5.4 Commit with message `[chore] initialize expo + sqlite project`
